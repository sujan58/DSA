import java.util.Scanner; public class Main {    public static void main(String[] args) {        Scanner sc = new Scanner(System.in);        if (sc.hasNextInt()) {            int t = sc.nextInt();            while (t > 0) {                int n = sc.nextInt();                int h = sc.nextInt();                int l = sc.nextInt();                int rowCapable = 0;                int colCapable = 0;                int validNumbers = 0;                for (int i = 0; i < n; i++) {                    int val = sc.nextInt();                    boolean canBeRow = (val <= h);                    boolean canBeCol = (val <= l);                    if (canBeRow || canBeCol) {                        validNumbers++;                        if (canBeRow) {                            rowCapable++;                        }                        if (canBeCol) {                            colCapable++;                        }                    }                }                int maxPairs = validNumbers / 2;                int ans = Math.min(maxPairs, Math.min(rowCapable, colCapable));                System.out.println(ans);                t--;            }        }        sc.close();    }}