import java.io.BufferedReader;import java.io.InputStreamReader;import java.io.IOException;import java.util.StringTokenizer; public class Main {    static BufferedReader reader = new BufferedReader(new InputStreamReader(System.in));    static StringTokenizer tokenizer;     static String getNextToken() throws IOException {        while (tokenizer == null || !tokenizer.hasMoreTokens()) {            String line = reader.readLine();            if (line == null) return null;            tokenizer = new StringTokenizer(line);        }        return tokenizer.nextToken();    }     static int getNextInt() throws IOException {        return Integer.parseInt(getNextToken());    }     static long getNextLong() throws IOException {        return Long.parseLong(getNextToken());    }     public static void main(String[] args) throws IOException {        String testCasesStr = getNextToken();        if (testCasesStr == null) return;                int totalTestCases = Integer.parseInt(testCasesStr);        StringBuilder outputBuilder = new StringBuilder();         while (totalTestCases > 0) {            int n = getNextInt();            long[] elements = new long[n];                        for (int i = 0; i < n; i++) {                elements[i] = getNextLong();            }                        for (int i = 0; i < n; i++) {                getNextLong();            }                        long[] adjacentGcds = new long[n - 1];            for (int i = 0; i < n - 1; i++) {                adjacentGcds[i] = computeGcd(elements[i], elements[i + 1]);            }                        int maxOperations = 0;                        if (elements[0] > adjacentGcds[0]) {                maxOperations++;            }                        for (int i = 1; i < n - 1; i++) {                long currentGcd = computeGcd(adjacentGcds[i - 1], adjacentGcds[i]);                long requiredLcm = (adjacentGcds[i - 1] / currentGcd) * adjacentGcds[i];                                if (elements[i] > requiredLcm) {                    maxOperations++;                }            }                        if (elements[n - 1] > adjacentGcds[n - 2]) {                maxOperations++;            }                        outputBuilder.append(maxOperations).append("\n");            totalTestCases--;        }                System.out.print(outputBuilder.toString());    }     private static long computeGcd(long a, long b) {        while (b != 0) {            long temp = b;            b = a % b;            a = temp;        }        return a;    }}