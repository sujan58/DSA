import sys # Precompute primes using Sieve of EratosthenesMAX_VAL = 150000is_prime = [True] * (MAX_VAL + 1)is_prime[0] = is_prime[1] = Falseprimes = [] for p in range(2, MAX_VAL + 1):    if is_prime[p]:        primes.append(p)        # Mark multiples as non-prime        for i in range(p * p, MAX_VAL + 1, p):            is_prime[i] = False def solve():    # Fast I/O    input_data = sys.stdin.read().split()    if not input_data:        return        t = int(input_data[0])    out = []        for i in range(1, t + 1):        n = int(input_data[i])        res = []                # Construct the sequence: a_i = prime[i] * prime[i+1]        for j in range(n):            res.append(str(primes[j] * primes[j+1]))                    out.append(" ".join(res))            sys.stdout.write("\n".join(out) + "\n") if __name__ == '__main__':    solve()